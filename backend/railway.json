{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py makemigrations --no-input && python manage.py migrate --no-input && gunicorn general.wsgi",
    "restartPolicyType": "ON_FAILURE"
  }
}